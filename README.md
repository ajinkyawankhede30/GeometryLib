# GeometryLib

# Clone the repo
git clone https://github.com/yourusername/GeometryLib.git
cd GeometryLib

# Create a build directory
mkdir build && cd build

# Run CMake
cmake ..

# Build the project
cmake --build .
