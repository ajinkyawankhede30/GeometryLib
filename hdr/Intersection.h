#pragma once

#include "GeoUtils.h"

namespace geometry {

	bool intersection(const Vector<int, DIM2>& a, const Vector<int, DIM2>& b, const Vector<int, DIM2>& c, const Vector<int, DIM2>& d);

	
}
