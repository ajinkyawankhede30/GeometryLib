#pragma once
//#include "Point.h"
#include "VectorClass.h"

namespace geometry
{
	//template <typename T>
	int areaofTriangle2D(const Vector<int, DIM2>& a, const   Vector<int, DIM2>& b, const   Vector<int, DIM2>& c);

	//template <typename T>
	double areaofTriangle3D(const Vector<int, DIM3>& a, const   Vector<int, DIM3>& b, const   Vector<int, DIM3>& c);

	//template <typename T>
	int orientation2D(const Vector<int, DIM2>& a, const   Vector<int, DIM2>& b, const   Vector<int, DIM2>& c);

	//template <typename T>
	int orientation3D(const Vector<int, DIM3>& a, const   Vector<int, DIM3>& b, const   Vector<int, DIM3>& c);

	//bool isLeft(const Point2D& a, const Point2D& b, const Point2D& c);
	//bool isLeft(const Point3D& a, const Point3D& b, const Point3D& c);
	//
	//bool isRight(const Point2D& a, const Point2D& b, const Point2D& c);
	//bool isRight(const Point3D& a, const Point3D& b, const Point3D& c);
	//
	//bool isLeftorBeyond(const Point2D& a, const Point2D& b, const Point2D& c);
	//bool isLeftorBeyond(const Point3D& a, const Point3D& b, const Point3D& c);

	//bool isLeftorBetween(const Point2D& a, const Point2D& b, const Point2D& c);
	//bool isLeftorBetween(const Point3D& a, const Point3D& b, const Point3D& c);

}				 
